{
  "name": "@flightops/cloudflare-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply BOOKINGS_DB --local",
    "d1:migrate:remote": "wrangler d1 migrations apply BOOKINGS_DB --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260303.0"
  }
}
